"use strict";var express=require("express"),app=express();require("dotenv").config();var port=process.env.PORT||5e3,cors=require("cors");app.use(express.json()),app.use(cors());var usersRoute=require("./routes/usersRoute"),petRoute=require("./routes/petRoute");app.use("/users",usersRoute),app.use("/pets",petRoute),app.listen(port,function(){console.log("Listening on port ".concat(port,"..."))});